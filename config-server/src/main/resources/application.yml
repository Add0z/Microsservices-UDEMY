server:
  port: 8888
spring:
  application:
    name: spring-config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Add0z/ms-udemy-config-server
          default-label: main
          search-paths: 'greeting-service*'
